# Options Deep Environment Configuration
# Copy this file to .env and fill in your actual values
# IMPORTANT: Never commit .env to version control - it contains secrets!

# ============================================================================
# ENVIRONMENT SELECTION
# ============================================================================
# Specifies which environment configuration to load from src/config/environment_configs/
# Valid values: local, dev, prod
# Default: local
ENVIRONMENT=local

# ============================================================================
# DATABASE CONFIGURATION
# ============================================================================
# Database password used for all database connections
# This password is used for both equities and algorithm databases
# Security: Use a strong, unique password for each environment
OPTIONS_DEEP_DATA_WAREHOUSE_PASSWORD=your_secure_password_here

# Database connection details are configured in:
# - src/config/environment_configs/local.json  (for ENVIRONMENT=local)
# - src/config/environment_configs/dev.json    (for ENVIRONMENT=dev)
# - src/config/environment_configs/prod.json   (for ENVIRONMENT=prod)
#
# Local environment databases:
# - equities-local (username: e_user, host: localhost, port: 5432)
# - algorithms-local (username: postgres, host: localhost, port: 5432)

# ============================================================================
# EXTERNAL API KEYS
# ============================================================================
# NASDAQ API Key for accessing company and market data
# Obtain from: https://data.nasdaq.com/
NASDAQ_API_KEY=your_nasdaq_api_key_here

# ============================================================================
# ADDITIONAL NOTES
# ============================================================================
# 1. Database Setup Required:
#    Before running the application, ensure PostgreSQL databases are created:
#    - CREATE DATABASE "equities-local" OWNER e_user;
#    - CREATE DATABASE "algorithms-local" OWNER postgres;
#
# 2. Database Users:
#    Ensure database users exist with proper permissions:
#    - Local: e_user (for equities), postgres (for algorithms)
#    - See: src/database/database-setup.md for detailed setup instructions
#
# 3. Environment Configs:
#    Non-sensitive configuration (host, port, database names, usernames)
#    is stored in JSON files at src/config/environment_configs/
#
# 4. Security Best Practices:
#    - Never commit this file with real values
#    - Use different passwords for each environment
#    - Rotate API keys regularly
#    - Use secrets management in production (AWS Secrets Manager, etc.)
#
# 5. Troubleshooting:
#    - Verify all variables are set: env | grep -E "(ENVIRONMENT|OPTIONS_DEEP|NASDAQ)"
#    - Check documentation: src/config/ENVIRONMENT.md
#    - Database setup guide: src/database/database-setup.md
