Single-database configuration for Algorithms database migrations.

This directory contains Alembic migration files for the algorithms database only.

To run migrations:
1. Set environment variables:
   export ENVIRONMENT=local
   export OPTIONS_DEEP_DATA_WAREHOUSE_PASSWORD=your_password

2. From the project root, run:
   cd src/database/algorithms
   alembic upgrade head

3. To generate new migrations:
   alembic revision --autogenerate -m "Description of changes"